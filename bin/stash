#!/usr/bin/env bash

tmpfile="/tmp/${0##*/}.tmp"

if [ -t 0 ]; then
  # stdin is a terminal, read input like cat
  cat > "$tmpfile"
else
  # stdin is not a terminal (being piped), output stored content
  cat "$tmpfile"
fi